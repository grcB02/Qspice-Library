# --- QSpice Simulation Data ---
# These files are often massive and can be regenerated by running the simulation
# 1. Ignore all .qraw files everywhere by default
*.qraw

# 2. Allow .qraw files ONLY if they are inside a folder named "Results"
!**/Results/*.qraw
